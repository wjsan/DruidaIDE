Imports Code_Editor_For_Arduino.CodeObjects
Imports Code_Editor_For_Arduino.CodeElementIndetfier

Public Class GlobalCodeScope

    'Public Function getGlobalScope() As CodeScope
    '    Dim globalScope = New CodeScope("Global")
    '    For Each type In knownTypes
    '        globalScope.Types.Add(New ItemType(type, globalScope))
    '    Next
    'End Function

End Class
